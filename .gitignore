# ── Secrets — NEVER commit ─────────────────────────────────────────────
.env
.env.local
.env.*.local
*.local.env
# (keep .env.example / .env.local.template — they are safe templates)
keys.json
*.keys.json
google-services.json
**/google-services.json
GoogleService-Info.plist
**/GoogleService-Info.plist
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision
serviceAccount*.json
**/service-account*.json

# ── Unused second native app (Manifold's, carries foreign Firebase files) ─
oracle/mani/

# ── Dependencies & build output ────────────────────────────────────────
node_modules/
dist/
build/
out/
.next/
.expo/
coverage/
*.tsbuildinfo

# ── Native (generated by `expo prebuild --clean`) ──────────────────────
oracle/native/ios/
oracle/native/android/

# ── Solidity / Foundry ─────────────────────────────────────────────────
cache/
artifacts/
broadcast/
predikt-contracts/**/out/
predikt-relay/*.sqlite
predikt-relay/*.db

# ── Misc ───────────────────────────────────────────────────────────────
*.log
.DS_Store
.turbo/
.crush/
addresses.*.json
!addresses.amoy.json
