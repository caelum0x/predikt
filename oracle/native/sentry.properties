defaults.url=https://sentry.io/
# TODO(SHIP): set to the real Predikt Sentry org, or remove Sentry entirely
# (see app.config.js @sentry plugin + Sentry.init in App.tsx).
defaults.org=REPLACE_WITH_SENTRY_ORG
defaults.project=react-native
# Using SENTRY_AUTH_TOKEN environment variable
